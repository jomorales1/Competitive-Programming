#include <bits/stdc++.h>

using namespace std;

int main() {
    string spell;
    cin >> spell;
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        string word;
        cin >> word;
        int m = word.length();
        for (int j = 0; j < n - m - 1; j++) {
            //if (spell.substr(j, m) == word)
        }
    }
    return 0;
}