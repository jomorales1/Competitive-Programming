#include <bits/stdc++.h>

using namespace std;

int main() {
    int left, right;
    cin >> left >> right;
    if (left == right) {
        cout << left;
    } else {
        cout << 2 << '\n';
    }
    return 0;
}